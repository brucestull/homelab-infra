---
# Ansible inventory — the machines this control node manages.
# Structure: groups contain hosts; hosts have connection variables.

all:
  children:
    dns_servers:
      hosts:
        script-zappy:
          ansible_host: 192.168.1.229
          ansible_user: flynntknapp
          ansible_ssh_private_key_file: ~/.ssh/id_ed25519_react_nodes_to_script_zappy
